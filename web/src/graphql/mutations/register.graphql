mutation Register($username: String!, $password: String!) {
    register(user: { username: $username, password: $password }) {
        errors {
            field
            message
        }
        user {
            ...UserFragment
        }
    }
}
